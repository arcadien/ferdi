# Requirements

This file documents all user requests as formal requirements for the **ferdi** project.
Each requirement is assigned a unique ID (REQ-NNN) and linked to a technical specification in `technical-specifications.md`.

Requirements are created via the `/document` command and updated via `/update-status`.

---
